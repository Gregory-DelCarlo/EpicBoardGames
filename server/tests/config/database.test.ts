import * as db from './database';
import { describe, expect, it} from '@jest/globals';

describe("Memory server connection tests", () => {
    describe("connect test", () => {
        it("should connect to the memory server", ()=>{
            
        });
    });
});